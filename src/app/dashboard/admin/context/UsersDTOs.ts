// to get from (form) but Aother interface to get from  Api 

export interface CreateUsers{
    name:string,
      email:string,
      password: string,
      gender:string,
      image: object,
      user_type:string,
}